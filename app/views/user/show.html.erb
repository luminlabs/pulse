<div class="content">

<h3><%= @student.name %></h3>
  

 <%= button_to('Edit User', edit_student_path(@student), :method => :get, :class => "form-element" ) %>
 <%= button_to('Delete User', @student, :method => :delete, :confirm => 'Are you sure?', :class => "form-element") %>
</div>


 